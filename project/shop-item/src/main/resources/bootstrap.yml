spring:
  cloud:
    config:
      uri: http://localhost:9999
      profile: eureka-client,shop-item,mq
      name: application
  rabbitmq:
    listener:
      simple:
        acknowledge-mode: manual
